- name: Install docker
  apt:
    name:
      - docker.io
      - docker-compose
      - docker-buildx
    state: present
    update_cache: true

- name: Enable docker
  systemd:
    name: docker
    enabled: true
    state: started
